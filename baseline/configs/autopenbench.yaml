agent:
  agent_code: baseline/agents/autopenbench.py
  # Prompt construction intentionally lives in baseline/agents/autopenbench.py
  # so it can mirror upstream AutoPenBench and render runtime target_subnet.
  step_limit: 30
  model_kwargs:
    temperature: 0.7

docker_environment:
  image: "autopenenv"
  network_name: "ctfnet"
  timeout: 150

challenge_client:
  benchmark_root: "./bench_hub/benchmarks"
  run_mode: "remote"
  server_url: "http://10.1.2.146:8000"
  use_ssh_tunnel: false

logging:
  level: "DEBUG"
  log_dir: "baseline/logs/v2"
  console_output: true
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
